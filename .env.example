# =============================================================================
# Environment Variables Template for SBA Loan ML System
# =============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp .env.example .env
#
# IMPORTANT: Never commit .env file to version control!
# The .env file is in .gitignore and .dockerignore
# =============================================================================

# -----------------------------------------------------------------------------
# AWS Configuration
# -----------------------------------------------------------------------------
# AWS region where your resources are deployed
AWS_REGION=ap-south-2

# S3 bucket name for ML artifacts storage
S3_BUCKET_NAME=sba-credit-risk-artifacts-sagar

# AWS credentials (get these from AWS IAM Console)
# For local development: aws configure
# For production: Use IAM roles instead of hardcoded credentials
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Set to 'true' for cloud deployment (downloads artifacts from S3)
# Set to 'false' for local development (uses local artifacts)
SYNC_FROM_S3=false

# API server configuration
API_HOST=0.0.0.0
API_PORT=8000

# Streamlit UI configuration
# For local development: http://localhost:8000
# For Docker Compose: http://api:8000
# For AWS ECS: http://sba-loan-api.local:8000 (via service discovery)
API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# GitHub Actions Secrets (Configure in GitHub repository settings)
# -----------------------------------------------------------------------------
# Go to: Repository Settings > Secrets and variables > Actions > New secret
#
# Required secrets:
#   - AWS_ACCESS_KEY_ID: Your AWS access key ID
#   - AWS_SECRET_ACCESS_KEY: Your AWS secret access key
#   - AWS_ACCOUNT_ID: Your 12-digit AWS account ID
#
# Optional secrets:
#   - CODECOV_TOKEN: Token for uploading test coverage to Codecov
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Local Development Quick Start
# -----------------------------------------------------------------------------
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Update AWS credentials in .env file
#
# 3. For local development without S3:
#    - Keep SYNC_FROM_S3=false
#    - Run: python run_pipeline.py
#    - Start API: uvicorn src.api.main:app --reload
#    - Start UI: streamlit run app.py
#
# 4. For testing S3 sync:
#    - Upload artifacts: python scripts/push_artifacts.py
#    - Set SYNC_FROM_S3=true in .env
#    - Start API: uvicorn src.api.main:app --reload
#
# 5. For Docker local testing:
#    - Run: docker-compose up --build
#    - Access API: http://localhost:8000
#    - Access UI: http://localhost:8501
# -----------------------------------------------------------------------------
